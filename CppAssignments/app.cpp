#include <iostream>
#include"Pointers.h"
#include "MultiThreading.h"

int main(int argc,char* argv[])
{
	std::cout << "Hello World!\n";
	/*std::thread thread1(multithreading_producer_conditional,15);
	std::thread thread2(multithreading_consumer_conditional);
	std::thread thread3(multithreading_consumer_conditional);

	thread1.join();
	thread2.join();
	thread3.join()*/;

	return 0;
}
